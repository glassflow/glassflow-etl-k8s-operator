name: Merge to Main

on:
  push:
    branches:
      - main

jobs:
  test-operator:
    uses: ./.github/workflows/test-operator.yml

  build-operator-image:
    needs: [test-operator]
    uses: ./.github/workflows/build-image.yml
    with:
      push: true
    secrets: inherit